import styled from 'styled-components'
import { Button } from '../Button'

export const PhotoButton = styled(Button)`
  position: fixed;
  bottom: 15px;
  right: 30px;
  z-index: 3;
  height: auto;
`
